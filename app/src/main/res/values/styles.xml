<resources>
    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <!--Dialgo style -->
    <style name="DialogStyle" parent="Theme.AppCompat.Light.Dialog.Alert">
        <item name="android:background">@color/colorPrimary</item>
    </style>
    <style name="DialogEditTextTheme">
        <item name="colorControlActivated">@color/colorDialogButton</item>
        <item name="android:textSelectHandle">@color/colorDialogButton</item>
        <item name="android:textCursorDrawable">@color/colorDialogButton</item>
    </style>
</resources>